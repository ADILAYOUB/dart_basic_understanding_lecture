// ignore: file_names
void main() {
  int result = perimeterOfTriangle(10, 12, 8);
  print(result);
}

// find perimeter of a tringale

int perimeterOfTriangle(int a, int b, int c) {
  // add the side of a triangle to give the perimeter
  int sum = a + b + c;
  return sum;
}
